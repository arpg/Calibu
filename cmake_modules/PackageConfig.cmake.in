# This file is populated by install_package.cmake
# install_package.cmake

# Compute paths
get_filename_component( PACKAGE_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH )
SET( @PROJECT_NAME@_INCLUDE_DIRS "@PACKAGE_INCLUDE_DIRS@" )
SET( @PROJECT_NAME@_INCLUDE_DIR  "@PACKAGE_INCLUDE_DIRS@" )

# Library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET "@PACKAGE_LIB_NAME@" AND NOT @PROJECT_NAME@_BINARY_DIR)
  include( "${PACKAGE_CMAKE_DIR}/@PROJECT_NAME@Targets.cmake" )
endif()

SET(@PROJECT_NAME@_LIBRARIES @PACKAGE_LINK_LIBS@)
SET(@PROJECT_NAME@_LIBRARY @PACKAGE_LIBRARY@)
SET(@PROJECT_NAME@_INCLUDE_DIRS @PACKAGE_INCLUDE_DIRS@)
SET(@PROJECT_NAME@_LINK_DIRS @PACKAGE_LINK_DIRS@)
